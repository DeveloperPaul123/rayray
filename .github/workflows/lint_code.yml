name: cpp-lint

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: action-cpp-lint
      uses: CyberZHG/github-action-cpp-lint@0.0.2
